<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog website</title>
    <link rel="stylesheet" href="/main.css">
    


</head>
<body>
    <nav class="navbar">
        <div class="container-fluid">
          <div  >
            
             <h1 class="navbar-brand"> 🅱️ Blogging website</h1> 
          </div>
        </div>
      </nav>
    
    <form action="/submit" method="POST">
        <div class="heading">
        <h1 >Create a New Blog Post</h1>
    </div>
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" required>
        
        <label for="title">Author:</label>
        <input type="text" id="author" name="author" required>

        <label for="content">Content:</label>
        <textarea id="content" name="content" rows="10" required></textarea>
        
        <input type="submit" value="Submit">
    </form>
   

    <div class="posts">
      <div class="post-heading ">
        <h1>Your Blogs</h1> 
      </div>
     <%  if(locals.posts){%>
       
          
        <ol>
         
          <% for(let i =0; i<posts.length;i++){ %> 
            <div class="newPost"> 
            <li>
              <h1 class="post-title"><%= posts[i].title %> </h1>
               <p class="post-content"><%= posts[i].content %></p>
               <h5>By: <%=posts[i].author%> </h5>
            </li>
            
            <a class="edit-link" href="/edit-post/<%=posts[i].id  %>">Edit</a>
            <!-- here we send the index(i) of the post in every link of edit by this we will knew that which post have to edit when we do get request -->
            <a class="delete-link" href="/delete-post/<%=posts[i].id %>">Delete</a>
           <% }%>
         </div> 
          
         
        </ol>
      <% } else{%>
        <h2 class="noBlogHeading">No Blog posted yet</h2>
        <% } %>
    </div>
   
</body>
</html>